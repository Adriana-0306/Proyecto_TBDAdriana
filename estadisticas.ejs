<!DOCTYPE html>
<html>
<head>
  <!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Estadísticas de Equipos Médicos | GEB</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/styles.css">
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f5f9fc;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
      margin: 0;
    }
    
    main {
      max-width: 1200px;
      margin: 20px auto;
      padding: 20px;
      flex: 1;
      width: 100%;
    }
    
    h1 {
      color: #2c3e50;
      margin-bottom: 30px;
      text-align: center;
    }
    
    .stats-container {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 20px;
      margin-top: 30px;
    }
    
    .stat-card {
      background: white;
      border-radius: 10px;
      padding: 25px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
      transition: transform 0.3s;
    }
    
    .stat-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 6px 12px rgba(0,0,0,0.15);
    }
    
    .stat-value {
      font-size: 2.5rem;
      font-weight: 700;
      color: #2980b9;
      margin: 10px 0;
    }
    
    .stat-label {
      color: #7f8c8d;
      font-size: 1.1rem;
    }
    
    .chart-container {
      margin-top: 40px;
      background: white;
      border-radius: 10px;
      padding: 25px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }
  </style>
</head>
<body>
  <%- include('partials/navbar') %>

  <main>
    <h1><i class="fas fa-chart-bar"></i> Estadísticas de Equipos Médicos</h1>
    
    <div class="stats-container">
      <div class="stat-card">
        <div class="stat-value"><%= totalEquipos %></div>
        <div class="stat-label">Total de Equipos Registrados</div>
      </div>
      
      <div class="stat-card">
        <div class="stat-value"><%= promedioDias %> días</div>
        <div class="stat-label">Promedio hasta próximo mantenimiento</div>
      </div>

</body>
</html>
